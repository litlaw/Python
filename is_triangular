def is_triangular(k):

    """

    k, a positive integer

    returns True if k is triangular and False if not

    """
    arr =[]
    for i in range (k):
        if k == 1:
            return True
        elif i*(i+1)/2 == k:
            return True
    return False
